import os
import math

def my_func(y: float) -> float:
    """Multiplies y by 2."""
    return y * 2

class MyClass:
    """This is a sample class."""

    def __init__(self, name: str):
        """Initialize with name."""
        self.name = name

    def greet(self) -> str:
        """Returns a greeting string."""
        return f"Hello, {self.name}"
